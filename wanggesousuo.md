# 网格搜索算法说明

## 一、问题背景与优化任务

静电除尘器（ESP）的运行优化涉及四个可调操作变量——电场 1\&2 平均二次电压 $\bar{U}_1$、电场 3\&4 平均二次电压 $\bar{U}_2$、电场 1\&2 平均振打周期 $\bar{T}_1$、电场 3\&4 平均振打周期 $\bar{T}_2$。在给定工况（温度 $H$、入口粉尘浓度 $C$ 固定于聚类中心，烟气流量 $Q$ 固定于工况均值）下，搜索这四个变量的最优组合，是本文第二问的核心任务。

本文先后构造了两个优化问题：

| 脚本 | 问题类型 | 目标函数 | 约束 | 建模方式 |
|------|----------|----------|------|----------|
| `pro2opt.m` | 单目标约束优化 | $\min P$ | $\eta \ge \eta_{\min}$（达标排放） | RF 预测 $\eta$，RF 预测 $P$ |
| `pro2opt_lease.m` | 双目标无约束 Pareto 优化 | $\min [P,\; C_{\text{out}}]$ | 无硬约束 | RF 预测 $\eta$，Poly 预测 $P$ |

两者均采用**网格搜索（Grid Search）**作为寻优算法。

---

## 二、网格搜索原理

### 2.1 基本思想

网格搜索是一种确定性全局优化方法。其核心思路是将搜索空间中每个连续决策变量离散化为等间距格点，在所有格点的直积（Cartesian product）上逐点评估目标函数，从中选取最优者。

设决策向量 $\mathbf{x} = (x_1, x_2, \dots, x_d) \in \mathbb{R}^d$，每个分量 $x_i$ 在其取值区间 $[a_i, b_i]$ 上均匀划分为 $n_i$ 个格点：

$$
x_i^{(j)} = a_i + \frac{j-1}{n_i-1}(b_i - a_i), \quad j = 1, 2, \dots, n_i
$$

则全体候选解构成有限格点集 $\mathcal{G} = \{x_1^{(j_1)}\} \times \{x_2^{(j_2)}\} \times \cdots \times \{x_d^{(j_d)}\}$，总候选数为 $\prod_{i=1}^d n_i$。网格搜索即在此离散集上穷举评估，返回最优解：

$$
\mathbf{x}^* = \arg\min_{\mathbf{x} \in \mathcal{G}} f(\mathbf{x})
$$

### 2.2 算法性质

- **确定性**：不含随机成分，每次运行结果严格一致。
- **全局性**：在离散化精度内保证找到全局最优，不会陷入局部极值。
- **可并行性**：各格点评估互不依赖，天然适合并行计算。
- **维数灾难**：候选数随维数 $d$ 指数增长（$O(n^d)$），故仅适用于低维问题。
- **精度–成本权衡**：格点越密，解越精确，但计算量以 $n^d$ 增长。

---

## 三、本文的具体实现

### 3.1 决策变量与搜索空间

四个决策变量在各自工况的**历史数据范围**内搜索：

$$
\bar{U}_1 \in [\min_k \bar{U}_1, \max_k \bar{U}_1],\quad
\bar{U}_2 \in [\min_k \bar{U}_2, \max_k \bar{U}_2]
$$
$$
\bar{T}_1 \in [\min_k \bar{T}_1, \max_k \bar{T}_1],\quad
\bar{T}_2 \in [\min_k \bar{T}_2, \max_k \bar{T}_2]
$$

其中 $\min_k, \max_k$ 表示第 $k$ 个工况（$k=1,2,3,4$）内该变量的最小与最大值。以历史数据定界，既不超出装备物理能力，也避免在无意义的区域浪费搜索。

### 3.2 单目标方案（`pro2opt.m`）：两阶段逐次加密

由于单目标问题是约束优化（$\eta \ge \eta_{\min}$），可行域可能狭窄，直接粗搜可能遗漏可行区。故采用**粗搜 → 局域加密**两阶段策略：

1. **粗搜阶段**：每维取 $n_{\text{coarse}} = 20$ 个格点，四维直积共 $20^4 = 1.6\times 10^5$ 个候选解。在所有候选解中筛选满足 $\eta \ge \eta_{\min}$ 的可行点，取其中 $P$ 最小者为粗搜最优 $\mathbf{x}_{\text{coarse}}$。

2. **加密阶段**：以粗搜最优点 $\mathbf{x}_{\text{coarse}}$ 为中心，在其各分量 $\pm 10\%$ 范围内再次划分 $n_{\text{fine}} = 20$ 个格点（亦 $1.6\times 10^5$ 候选），在更细的网格上重新搜索。加密范围取 $\max(a_i,\; x_{\text{coarse},i}(1-\delta))$ 至 $\min(b_i,\; x_{\text{coarse},i}(1+\delta))$，$\delta=0.10$，确保不越界。

两阶段合计评估约 $3.2\times 10^5$ 个候选解，在精度与计算成本间取得平衡。

### 3.3 双目标方案（`pro2opt_lease.m`）：单阶段细网格 + Pareto 支配筛选

双目标优化不设排放约束，转而同时最小化 $P$ 与 $C_{\text{out}}$，输出 Pareto 前沿供决策者权衡取舍。具体流程：

1. **网格生成**：每维取 $n = 25$ 格点，共 $25^4 \approx 3.9\times 10^5$ 个候选解。

2. **目标评估**：对每个候选解，调用随机森林 $f(\mathbf{x})$ 预测脱除效率 $\eta$，换算出口浓度 $C_{\text{out}} = C_k \times 1000 \times (1 - \eta / 100)$；调用二次多项式 $g_k(\cdot)$ 预测总功耗 $P$。

3. **Pareto 支配判别**：在全体 $3.9\times 10^5$ 个 $(P, C_{\text{out}})$ 对上执行双目标非支配排序（$O(n\log n)$ 算法），筛选出 Pareto 非劣解集，构成该工况的 Pareto 前沿。

### 3.4 计算规模概览

| 项目 | `pro2opt.m` | `pro2opt_lease.m` |
|------|-------------|-------------------|
| 网格策略 | 粗搜 20⁴ + 加密 20⁴ | 单次 25⁴ |
| 候选数/工况 | $1.6\times 10^5 \times 2$ | $3.9\times 10^5$ |
| 四工况总候选 | $\sim 1.3\times 10^6$ | $\sim 1.6\times 10^6$ |
| 目标评估耗时 | RF 预测微秒级 | RF + Poly 预测微秒级 |

代理模型（RF、多项式）的单次推理为微秒量级，四工况全搜索可在数秒内完成，计算开销远低于迭代式进化算法。

---

## 四、为何选择网格搜索——与替代方法的比较

### 4.1 替代方法简述

对于本问题的低维连续优化，理论上可选的方法包括：

| 方法 | 类型 | 梯度需求 | 全局性 | 典型调用次数 |
|------|------|----------|--------|-------------|
| 网格搜索 | 确定性穷举 | 无 | 离散化全局最优 | $n^d$（本文 $\sim 10^5$–$10^6$） |
| 遗传算法（GA） | 随机群体搜索 | 无 | 概率全局 | $10^3$–$10^5$ 代 $\times$ 种群 |
| 粒子群（PSO） | 随机群体搜索 | 无 | 概率全局 | $10^3$–$10^4$ 迭代 |
| 模拟退火（SA） | 随机单点搜索 | 无 | 概率全局 | $10^4$–$10^5$ 迭代 |
| 贝叶斯优化 | 序贯代理模型 | 无 | 概率全局 | $10^2$–$10^3$ 迭代 |
| 梯度下降（SQP, IPM） | 确定性迭代 | 需梯度 | 局部 | $10^2$–$10^3$ 迭代 |
| DIRECT | 确定性分区 | 无 | 确定性全局 | $10^3$–$10^4$ 分区 |

### 4.2 排除梯度法

随机森林模型 $f(\mathbf{x})$ 是分段常数的集成模型，梯度处处为零或不存在；二次多项式 $g_k(\mathbf{x})$ 虽有解析梯度，但 $f$ 不可导决定了整个目标函数链不可导。因此**基于梯度的优化方法（SQP、内点法、梯度下降族）不可用**。

### 4.3 排除进化算法的理由

遗传算法（GA）、粒子群算法（PSO）等随机元启发式是黑箱优化的常用选择，但本文未采用，原因有四：

1. **问题维度过低**。GA/PSO 的优势在高维（$d \ge 10$）才真正显现。本文仅 $d=4$，候选解空间的基数（$\sim 10^5$–$10^6$）完全在穷举可承受范围内。

2. **随机性不利于学术复现**。国赛论文要求结果可复验。网格搜索是确定性的——任何人以相同参数运行，必得相同结果。而 GA/PSO 依赖随机初始化和随机算子，不同运行可能收敛至不同解，需多次独立运行取统计值，增加解释负担。

3. **收敛不可靠**。进化算法仅保证概率收敛，对小可行域的约束优化（如 `pro2opt.m` 中 $\eta \ge \eta_{\min}$）可能整代无可行解而停滞。

4. **计算成本不具优势**。代理模型推理极快（微秒级），$10^5$ 次穷举评估的开销低于 GA 所需的上万次迭代加上种群维护开销。

### 4.4 网格搜索在本问题中的独特优势

| 优势 | 说明 |
|------|------|
| **确定性** | 结果完全可复现，符合学术规范 |
| **全局性** | 在离散化分辨率内保证全局最优，无遗漏风险 |
| **简单性** | 算法思想直观，无需调节种群大小、交叉概率、惯性权重等超参数 |
| **充分性** | $20^4$–$25^4$ 的分辨率对工程决策已足够 —— 电压 0.01 kV、振打周期 0.1 s 级别的精度远超实际操作精度 |
| **直接产出 Pareto 前沿** | 在双目标问题中，网格搜索天然生成高密度目标空间点云，经非支配排序即得光滑的 Pareto 前沿，无需多次运行或专门的多目标进化算子 |
| **边界安全** | 历史数据定界确保最优解落在装备实际运行范围内，不会给出物理上不可实现的"外推最优" |

### 4.5 两阶段加密策略的必要性（针对约束优化）

`pro2opt.m` 采用粗搜再加密，而非直接 40⁴ 的原因：

- 直接 $40^4 = 2.56\times 10^6$ 个候选，虽仍可承受，但其中绝大部分落在不可行域，评估效率低。
- 两阶段策略先在大范围筛选可行区的大致位置（粗搜），再将计算资源集中在最有希望的区域（加密），在相同总评估量下获得更高精度的解。
- 粗搜阶段的网格间距足以定位可行域——若某区域在 20⁴ 分辨率下无一可行点，则该区域在更密网格下大概率仍不可行。

---

## 五、结论

在四维有界连续空间上，以代理模型（随机森林 / 二次多项式）为目标函数的优化问题中，网格搜索是**最适配的方法**：

- 梯度法因 RF 不可导而排除；
- 进化算法因低维穷举可承受、随机性不可复现而舍弃；
- 网格搜索以确定性、全局性、实现简单性三重优势，在 $10^5$–$10^6$ 量级的候选评估中完成寻优，结果精确、可复现、易于在论文中完整描述。

对于论文正文中方法描述的措辞建议：

> 针对各工况在历史数据范围内构造 $n^4$（$n = 20\sim25$）均匀格点，逐点调用代理模型评估目标函数。单目标优化采用粗搜–加密两阶段策略，在满足排放约束的可行域内搜索最小功耗参数组合；双目标优化则在全体格点上执行 Pareto 支配判别（$O(n \log n)$），获得各工况的 Pareto 前沿。该方法具有确定性、全局性和可复现性，且计算成本在代理模型推理速度下完全可接受。

---

*本文档供论文撰写参考。算法细节见支撑材料 `pro2opt.m` 与 `pro2opt_lease.m`。*
